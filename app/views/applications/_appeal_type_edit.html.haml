%li{'data-id' => "#{appeal_type.id}"}
  %span{ class: 'edit_appeal_type', 'data-name' => 'name', 'data-resource' => 'appeal_type', 'data-type' => 'text', 'data-pk' => "#{appeal_type.id}", 'data-url' => '/appeal_types/update/' + appeal_type.id, 'data-emptytext' => 'укажите название для вида обращения...' }
    = appeal_type.name

:javascript

  $(document).ready(function() {
    $.fn.editable.defaults.mode = 'popup';
    $.fn.editable.defaults.ajaxOptions = {type: "PUT"};

    $('.edit_appeal_type').editable();

  })